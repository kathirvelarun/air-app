"""Run the live planner or a deterministic ready/no-agents scenario."""

from air_agent_app.agent.cli.planner import run_planner_cli

if __name__ == "__main__":
    raise SystemExit(run_planner_cli())
